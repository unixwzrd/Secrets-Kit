[pytest]
testpaths = tests
pythonpath =
    src
    tests
# Verbosity is set by the Makefile only: `make test` (loud) vs `make test-quiet` (CI).
addopts =
    --import-mode=importlib
    --strict-markers
