"""
secrets_kit.backends.oracle (reserved)

Placeholder package for a future :class:`~secrets_kit.backends.base.BackendStore`
implementation backed by Oracle Database.

**No runtime surface yet** — do not import SQLite or Keychain modules from here.
When implemented, this package should satisfy the same protocol as
``secrets_kit.backends.base.BackendStore`` without depending on SQLite
``user_version`` migrations or Keychain-specific wire formats.
"""
