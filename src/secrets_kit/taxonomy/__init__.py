"""
secrets_kit.taxonomy

Local taxonomy registry (entry types, kinds, tags) — canonical JSON authority.

Import submodules directly (e.g. secrets_kit.taxonomy.registry_store) to avoid
import cycles with backends.dispatch.
"""

__all__: list[str] = []
