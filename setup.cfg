# macOS wheels only: bundled helper is universal2 (lipo arm64+x86_64). Matches
# native_helper_src/Package.swift minimum (.macOS(.v13)).
# Build wheels on macOS. Do not publish linux/windows wheels for this package.
[bdist_wheel]
plat_name = macosx_13_0_universal2
